import { Box } from "@chakra-ui/react";

export default function PageNotFound() {
    return (
        <Box>
            I am not so sure about the page you are looking but it seems to not be on our site
        </Box>
    )
}